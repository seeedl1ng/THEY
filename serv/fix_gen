%%bash

SERVER_DIR="/content/drive/MyDrive/minecraft/cleanroom"
cd "$SERVER_DIR" || exit 1

echo "Stopping/cleaning broken world..."

# Сохраняем старый мир, а не удаляем
if [ -d world ]; then
  mv world "world_broken_$(date +%Y%m%d_%H%M%S)"
fi

# На всякий случай чистим временные/битые логи крашей
rm -rf crash-reports

echo "Current files:"
ls -la
